(function() { var fs = require("fs"); var lines = fs.readFileSync("D:/AiTools/freellmapi/client/src/pages/PlaygroundPage.tsx", "utf8").split("\\n"); var deleteIdx = lines.findIndex(function(l) { return l.includes("onClick= deleteProjectWithUndo(project.id, project)"); }); if (deleteIdx > 0) { var insertLines = ["                            onClick= createSession(project)", "                            title=New project chat\"", "                          >", "                            <Plus className=size-3.5\" />", "                          </Button>", ""]; var buttonStart = lines.findIndex(function(l, i) { return i < deleteIdx && l.includes("<Button\"); }); for (var i = insertLines.length - 1; i >= 0; i--) { lines.splice(buttonStart, 0, insertLines[i]); } fs.writeFileSync("D:/AiTools/freellmapi/client/src/pages/PlaygroundPage.tsx", lines.join("\\n")); console.log("Added project chat button"); } else { console.log("Pattern not found"); } })();
